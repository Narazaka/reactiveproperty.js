{
  "name": "reactiveproperty",
  "version": "1.0.2",
  "description": "ReactiveProperty for RxJS",
  "keywords": [
    "reactiveproperty",
    "rxjs"
  ],
  "main": "./dist/es2015/index.js",
  "types": "./dist/es2015/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dist": "rm -rf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint *.ts src/**/*.ts test/**/*.ts",
    "prepublish": "npm run dist"
  },
  "author": "Narazaka",
  "license": "MIT",
  "url": "https://github.com/Narazaka/reactiveproperty.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Narazaka/reactiveproperty.js.git"
  },
  "dependencies": {
    "rxjs": "^5.0.0"
  },
  "devDependencies": {
    "tslint": "^4.4.2",
    "typescript": "^3.9.3"
  }
}
